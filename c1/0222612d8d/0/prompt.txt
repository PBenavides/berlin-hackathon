## Task
Implement Sprint 2: Agent Action Types & Streaming Actions

## Branch
Create and switch to branch: `ai/sprint-2-agent-action-types-streaming-actions`
Run: `git checkout -b ai/sprint-2-agent-action-types-streaming-actions` (or `git checkout ai/sprint-2-agent-action-types-streaming-actions` if it already exists from a retry)

## Sprint Specification
{
  "sprint_number": 2,
  "name": "Agent Action Types & Streaming Actions",
  "goal": "Extend the Hermes agent with new action types (send email, respond to tenant, send financial report) and make all actions stream visibly in the UI.",
  "features": [
    {
      "id": "s2-f1",
      "name": "Agent Email-to-Vendor Action",
      "description": "Add a new agent write-tool that proposes sending an email to a vendor. When the agent determines a ticket requires vendor coordination, it drafts an email (recipient, subject, body) based on the vendor's contact info from context and the ticket details. The email is not actually sent \u2014 it's displayed as a proposed action in the UI showing exactly what would be sent. In auto-mode (Sprint 3), this action is auto-confirmed and logged as 'sent'.",
      "acceptance_criteria": [
        "The agent can propose a 'send vendor email' action with recipient, subject, and body fields",
        "The email content references the specific ticket details and vendor information from context",
        "The proposed email is visible in the ticket detail page as a distinct action card",
        "The action follows the existing proposal-envelope pattern (tool \u2192 confirmEndpoint \u2192 UI button)",
        "Email actions are logged in the audit trail"
      ],
      "touches": [
        "agent tools",
        "schemas",
        "ticket detail UI",
        "audit service"
      ],
      "risk_notes": "Must follow the existing write-tool pattern where actions are proposed, not executed directly."
    },
    {
      "id": "s2-f2",
      "name": "Agent Tenant-Response Action",
      "description": "Add a new agent write-tool that proposes a response to a tenant inquiry. The agent reads the tenant's question from the ticket, looks up the answer in the property context (FAQ, policies, rules), and drafts a response. The response includes the channel it would be sent through (matching the ticket's source channel \u2014 email reply, SMS, portal message). The drafted response is displayed as a proposed action.",
      "acceptance_criteria": [
        "The agent can propose a 'respond to tenant' action with the response text and target channel",
        "Responses are contextually accurate \u2014 they use information from the property's context layers",
        "The response channel matches the ticket's source (email \u2192 email reply, phone \u2192 SMS, portal \u2192 portal message)",
        "The proposed response is visible in the ticket detail page as a distinct action card",
        "Response actions are logged in the audit trail"
      ],
      "touches": [
        "agent tools",
        "schemas",
        "ticket detail UI",
        "audit service"
      ],
      "risk_notes": null
    },
    {
      "id": "s2-f3",
      "name": "Agent Owner-Financial-Report Action",
      "description": "Add a new agent write-tool that proposes sending financial information to a property owner. When a ticket involves cost reporting, invoice forwarding, or arrears notification, the agent compiles the relevant data (from vendor jobs, ticket history, unit rent data) and drafts a financial summary to send to the owner. The summary respects the owner's communication preferences from context.",
      "acceptance_criteria": [
        "The agent can propose a 'send owner report' action with recipient, format, and content summary",
        "Financial data is pulled from relevant sources (vendor job costs, unit rents, ticket history)",
        "The owner's preferred communication channel and language are respected",
        "The proposed report is visible in the ticket detail page as a distinct action card",
        "Financial actions are logged in the audit trail"
      ],
      "touches": [
        "agent tools",
        "schemas",
        "ticket detail UI",
        "audit service"
      ],
      "risk_notes": "Financial data must come from seed data \u2014 ensure seed includes enough cost/rent data for realistic reports."
    },
    {
      "id": "s2-f4",
      "name": "Agent Human-Escalation Action",
      "description": "Add a new agent write-tool that escalates a ticket to a human operator when the agent determines it cannot or should not resolve it autonomously. This happens when the ticket matches an escalation policy (e.g., legal issue, cost above threshold, safety concern). The escalation includes the reason, the specific policy that triggered it, and a recommended next step for the human.",
      "acceptance_criteria": [
        "The agent can propose an 'escalate to human' action with reason, triggering policy, and recommendation",
        "At least one demo ticket reliably triggers this escalation path",
        "Escalated tickets move to a visible 'needs human attention' state",
        "The escalation is visible in both the ticket detail and the overview/escalations page",
        "Escalation actions are logged in the audit trail"
      ],
      "touches": [
        "agent tools",
        "schemas",
        "escalation routes",
        "ticket detail UI",
        "overview UI"
      ],
      "risk_notes": "There's already an escalations page and route \u2014 integrate with existing infrastructure rather than building parallel."
    },
    {
      "id": "s2-f5",
      "name": "Streamable Action Cards in Ticket Detail",
      "description": "Redesign the ticket detail page to display agent actions as a real-time stream of action cards. As the agent works on a ticket, each step it takes (reading context, evaluating policies, drafting an email, proposing a vendor dispatch) appears as a new card that animates in. Each card shows: the action type (icon + label), a brief description of what the agent did or proposes, the timestamp, and the status (thinking \u2192 proposed \u2192 confirmed/rejected). This creates a visible narrative of the agent's reasoning process.",
      "acceptance_criteria": [
        "Agent actions appear as a vertical stream of cards on the ticket detail page",
        "Cards animate in as they happen (not all at once after completion)",
        "Each card shows action type, description, timestamp, and status",
        "Read actions (context lookup, ticket read) show as completed immediately",
        "Write actions (email, response, dispatch) show as 'proposed' with a confirm/reject option in manual mode",
        "The stream updates via SSE or polling without requiring a page refresh"
      ],
      "touches": [
        "ticket detail UI",
        "SSE/streaming infrastructure",
        "action schemas"
      ],
      "risk_notes": "The existing chat engine already uses SSE streaming \u2014 reuse that infrastructure for action streaming."
    }
  ],
  "success_criteria": [
    "The agent can resolve tickets using four distinct action types: vendor email, tenant response, owner report, human escalation",
    "Every agent action streams into the ticket detail page in real-time as visible action cards",
    "Running the pipeline on any demo ticket produces a visible, understandable trail of agent activity"
  ]
}

## Full Spec Constraints
[
  "The existing Hermes agent uses Google Gemini 2.5 Flash via OpenRouter \u2014 all new agent actions must work through this same LLM gateway",
  "Write tools follow the proposal-envelope pattern \u2014 they propose actions for human confirmation, never execute directly",
  "The SSE streaming infrastructure already exists in the chat engine \u2014 reuse it for action streaming rather than building a parallel system",
  "The seed service must remain idempotent \u2014 safe to run multiple times without duplicating data",
  "The frontend is a Next.js 15 app in a pnpm monorepo with shadcn/ui components \u2014 all new UI must use this stack",
  "The backend uses SQLAlchemy with Alembic migrations and PostgreSQL \u2014 new models require proper migrations",
  "All API responses must follow the existing standardized format with camelCase serialization",
  "The existing /dev/reset endpoint drops all tables \u2014 it must be extended to handle any new models/tables",
  "Context files use a 4-layer markdown format (vendor/owner/building/property) \u2014 additions must respect this structure",
  "The agent has a 50-turn budget per invocation (HERMES_MAX_TURNS) \u2014 batch processing must manage this per-ticket"
]

## Out of Scope
[
  "Actually sending real emails, SMS, or messages to external systems \u2014 all actions are simulated/displayed in the UI",
  "Multi-user authentication or role-based access \u2014 this is a single-presenter demo environment",
  "Production deployment or scaling \u2014 this runs locally or on a single demo instance",
  "Modifying the core Hermes agent LLM or switching providers",
  "Mobile responsiveness \u2014 the demo targets a laptop/projector presentation setup",
  "Internationalization beyond the existing German ticket content",
  "Integration with real Slack, email, or SMS providers",
  "Persistent demo state across server restarts \u2014 reset is expected after restart"
]

## Previous QA Bugs to Fix
The evaluator found these bugs in your previous attempt. Fix them ALL:

[
  {
    "id": "bug-001",
    "severity": "low",
    "category": "regression",
    "description": "Memory save/skip workflow: Ticket t-7832 (completed, vendor job closed) appears in the 'Memory writes pending' section on the overview page with 'memory pending' label, but the ticket detail page shows no save/skip controls anywhere \u2014 no buttons, no memory proposal UI. The memoryProposal field on the ticket is null. The workflow appears to only surface the pending state but has no UI path to act on it.",
    "steps_to_reproduce": [
      "Navigate to /overview",
      "Find 'Memory writes pending \u2014 vendor closed the job' section",
      "Click the 'Gegensprechanlage defekt \u2014 Einheit E1' link (ticket t-7832)",
      "Observe: ticket detail page loads with vendor job info but no 'Save to memory' or 'Skip' buttons"
    ],
    "expected": "Operator should see save/skip buttons to decide whether to write the vendor job outcome to context.md",
    "actual": "No memory action controls appear on the ticket detail page; memoryProposal is null in the API response",
    "file_hint": "frontend/apps/web/app/ticket/[id]/page.tsx"
  },
  {
    "id": "bug-002",
    "severity": "low",
    "category": "ux",
    "description": "Stale seeded error action cards visible before first re-run: On a fresh database (before reset), tickets t-7834 and t-7837 displayed an initial error action card 'get_ticket: error \u2014 Ticket GAR-121 not found' (using the ticket number instead of internal ID). These were seeded artifacts from a pre-fix run. After /api/dev/reset the issue disappears, but any deployment with old seeded data will confuse operators with visible error cards alongside correct ones.",
    "steps_to_reproduce": [
      "Deploy from a fresh seed without running /api/dev/reset",
      "Navigate to /ticket/t-7834",
      "Observe the first action card shows 'get_ticket: error \u2014 Ticket GAR-121 not found'"
    ],
    "expected": "No error action cards in freshly seeded database",
    "actual": "Error action card is visible (legacy from pre-Bug-003-fix data); correct card from second run appears alongside it",
    "file_hint": "backend/app/services/seed_service.py"
  },
  {
    "id": "bug-003",
    "severity": "low",
    "category": "ux",
    "description": "Chat at /chat does not use the selected property scope: When 'Gartenstra\u00dfe 42' is selected in the scope dropdown and user asks 'What is the phone number for this property?', Hermes responds with 'Please provide the property ID' instead of returning the phone number from context. The property scope selector appears non-functional for scoping the agent's context.",
    "steps_to_reproduce": [
      "Navigate to /chat",
      "Select 'Gartenstra\u00dfe 42' from the scope dropdown",
      "Ask: 'What is the phone number for this property?'",
      "Observe response: 'Please provide the property ID'"
    ],
    "expected": "Hermes uses the selected property scope and returns +49 30 5550 1042",
    "actual": "Hermes ignores the selected property and asks for a property ID",
    "file_hint": "backend/app/api/routes/chat.py or frontend/apps/web/app/chat/page.tsx"
  }
]

Evaluator notes: Sprint 2 is in strong shape. All four agent action types (vendor email, tenant response, owner financial report, human escalation) work end-to-end with streaming. The escalation flow is the most complex and works correctly: Confirm POSTs to /api/tickets/{id}/escalate with the real internal ID (Bug-003 fix confirmed), the ticket page shows the escalation notice after reload, and /escalations shows the 'Agent escalated' badge. Action cards render with proper type-specific styling (red border for escalations, amber for write proposals, muted for reads) and the 'Proposed (display only)' button correctly signals display-only actions. Code quality is clean with good separation between proposal factories and tool definitions. The three bugs found are all low severity — none block core sprint functionality. The memory workflow regression is the most notable gap: the 'memory pending' state surfaces in the overview but has no actionable UI on the ticket detail page.

## Instructions
1. Read CLAUDE.md and key source files to understand current patterns
2. Create the branch
3. Implement each feature in the order listed
4. Commit after each feature with format: [ai-builder/generator] description\nSprint: 2 | Feature: <id>
5. After ALL features are implemented, output the sprint contract as a ```json block

Set run_id to "20260426-031913" in the contract.
Set branch to "ai/sprint-2-agent-action-types-streaming-actions" in the contract.

---

## Task
Implement Sprint 2: Agent Action Types & Streaming Actions

## Branch
Create and switch to branch: `ai/sprint-2-agent-action-types-streaming-actions`
Run: `git checkout -b ai/sprint-2-agent-action-types-streaming-actions` (or `git checkout ai/sprint-2-agent-action-types-streaming-actions` if it already exists from a retry)

## Sprint Specification
{
  "sprint_number": 2,
  "name": "Agent Action Types & Streaming Actions",
  "goal": "Extend the Hermes agent with new action types (send email, respond to tenant, send financial report) and make all actions stream visibly in the UI.",
  "features": [
    {
      "id": "s2-f1",
      "name": "Agent Email-to-Vendor Action",
      "description": "Add a new agent write-tool that proposes sending an email to a vendor. When the agent determines a ticket requires vendor coordination, it drafts an email (recipient, subject, body) based on the vendor's contact info from context and the ticket details. The email is not actually sent \u2014 it's displayed as a proposed action in the UI showing exactly what would be sent. In auto-mode (Sprint 3), this action is auto-confirmed and logged as 'sent'.",
      "acceptance_criteria": [
        "The agent can propose a 'send vendor email' action with recipient, subject, and body fields",
        "The email content references the specific ticket details and vendor information from context",
        "The proposed email is visible in the ticket detail page as a distinct action card",
        "The action follows the existing proposal-envelope pattern (tool \u2192 confirmEndpoint \u2192 UI button)",
        "Email actions are logged in the audit trail"
      ],
      "touches": [
        "agent tools",
        "schemas",
        "ticket detail UI",
        "audit service"
      ],
      "risk_notes": "Must follow the existing write-tool pattern where actions are proposed, not executed directly."
    },
    {
      "id": "s2-f2",
      "name": "Agent Tenant-Response Action",
      "description": "Add a new agent write-tool that proposes a response to a tenant inquiry. The agent reads the tenant's question from the ticket, looks up the answer in the property context (FAQ, policies, rules), and drafts a response. The response includes the channel it would be sent through (matching the ticket's source channel \u2014 email reply, SMS, portal message). The drafted response is displayed as a proposed action.",
      "acceptance_criteria": [
        "The agent can propose a 'respond to tenant' action with the response text and target channel",
        "Responses are contextually accurate \u2014 they use information from the property's context layers",
        "The response channel matches the ticket's source (email \u2192 email reply, phone \u2192 SMS, portal \u2192 portal message)",
        "The proposed response is visible in the ticket detail page as a distinct action card",
        "Response actions are logged in the audit trail"
      ],
      "touches": [
        "agent tools",
        "schemas",
        "ticket detail UI",
        "audit service"
      ],
      "risk_notes": null
    },
    {
      "id": "s2-f3",
      "name": "Agent Owner-Financial-Report Action",
      "description": "Add a new agent write-tool that proposes sending financial information to a property owner. When a ticket involves cost reporting, invoice forwarding, or arrears notification, the agent compiles the relevant data (from vendor jobs, ticket history, unit rent data) and drafts a financial summary to send to the owner. The summary respects the owner's communication preferences from context.",
      "acceptance_criteria": [
        "The agent can propose a 'send owner report' action with recipient, format, and content summary",
        "Financial data is pulled from relevant sources (vendor job costs, unit rents, ticket history)",
        "The owner's preferred communication channel and language are respected",
        "The proposed report is visible in the ticket detail page as a distinct action card",
        "Financial actions are logged in the audit trail"
      ],
      "touches": [
        "agent tools",
        "schemas",
        "ticket detail UI",
        "audit service"
      ],
      "risk_notes": "Financial data must come from seed data \u2014 ensure seed includes enough cost/rent data for realistic reports."
    },
    {
      "id": "s2-f4",
      "name": "Agent Human-Escalation Action",
      "description": "Add a new agent write-tool that escalates a ticket to a human operator when the agent determines it cannot or should not resolve it autonomously. This happens when the ticket matches an escalation policy (e.g., legal issue, cost above threshold, safety concern). The escalation includes the reason, the specific policy that triggered it, and a recommended next step for the human.",
      "acceptance_criteria": [
        "The agent can propose an 'escalate to human' action with reason, triggering policy, and recommendation",
        "At least one demo ticket reliably triggers this escalation path",
        "Escalated tickets move to a visible 'needs human attention' state",
        "The escalation is visible in both the ticket detail and the overview/escalations page",
        "Escalation actions are logged in the audit trail"
      ],
      "touches": [
        "agent tools",
        "schemas",
        "escalation routes",
        "ticket detail UI",
        "overview UI"
      ],
      "risk_notes": "There's already an escalations page and route \u2014 integrate with existing infrastructure rather than building parallel."
    },
    {
      "id": "s2-f5",
      "name": "Streamable Action Cards in Ticket Detail",
      "description": "Redesign the ticket detail page to display agent actions as a real-time stream of action cards. As the agent works on a ticket, each step it takes (reading context, evaluating policies, drafting an email, proposing a vendor dispatch) appears as a new card that animates in. Each card shows: the action type (icon + label), a brief description of what the agent did or proposes, the timestamp, and the status (thinking \u2192 proposed \u2192 confirmed/rejected). This creates a visible narrative of the agent's reasoning process.",
      "acceptance_criteria": [
        "Agent actions appear as a vertical stream of cards on the ticket detail page",
        "Cards animate in as they happen (not all at once after completion)",
        "Each card shows action type, description, timestamp, and status",
        "Read actions (context lookup, ticket read) show as completed immediately",
        "Write actions (email, response, dispatch) show as 'proposed' with a confirm/reject option in manual mode",
        "The stream updates via SSE or polling without requiring a page refresh"
      ],
      "touches": [
        "ticket detail UI",
        "SSE/streaming infrastructure",
        "action schemas"
      ],
      "risk_notes": "The existing chat engine already uses SSE streaming \u2014 reuse that infrastructure for action streaming."
    }
  ],
  "success_criteria": [
    "The agent can resolve tickets using four distinct action types: vendor email, tenant response, owner report, human escalation",
    "Every agent action streams into the ticket detail page in real-time as visible action cards",
    "Running the pipeline on any demo ticket produces a visible, understandable trail of agent activity"
  ]
}

## Full Spec Constraints
[
  "The existing Hermes agent uses Google Gemini 2.5 Flash via OpenRouter \u2014 all new agent actions must work through this same LLM gateway",
  "Write tools follow the proposal-envelope pattern \u2014 they propose actions for human confirmation, never execute directly",
  "The SSE streaming infrastructure already exists in the chat engine \u2014 reuse it for action streaming rather than building a parallel system",
  "The seed service must remain idempotent \u2014 safe to run multiple times without duplicating data",
  "The frontend is a Next.js 15 app in a pnpm monorepo with shadcn/ui components \u2014 all new UI must use this stack",
  "The backend uses SQLAlchemy with Alembic migrations and PostgreSQL \u2014 new models require proper migrations",
  "All API responses must follow the existing standardized format with camelCase serialization",
  "The existing /dev/reset endpoint drops all tables \u2014 it must be extended to handle any new models/tables",
  "Context files use a 4-layer markdown format (vendor/owner/building/property) \u2014 additions must respect this structure",
  "The agent has a 50-turn budget per invocation (HERMES_MAX_TURNS) \u2014 batch processing must manage this per-ticket"
]

## Out of Scope
[
  "Actually sending real emails, SMS, or messages to external systems \u2014 all actions are simulated/displayed in the UI",
  "Multi-user authentication or role-based access \u2014 this is a single-presenter demo environment",
  "Production deployment or scaling \u2014 this runs locally or on a single demo instance",
  "Modifying the core Hermes agent LLM or switching providers",
  "Mobile responsiveness \u2014 the demo targets a laptop/projector presentation setup",
  "Internationalization beyond the existing German ticket content",
  "Integration with real Slack, email, or SMS providers",
  "Persistent demo state across server restarts \u2014 reset is expected after restart"
]

## Previous QA Bugs to Fix
The evaluator found these bugs in your previous attempt. Fix them ALL:

[
  {
    "id": "bug-001",
    "severity": "low",
    "category": "regression",
    "description": "Memory save/skip workflow: Ticket t-7832 (completed, vendor job closed) appears in the 'Memory writes pending' section on the overview page with 'memory pending' label, but the ticket detail page shows no save/skip controls anywhere \u2014 no buttons, no memory proposal UI. The memoryProposal field on the ticket is null. The workflow appears to only surface the pending state but has no UI path to act on it.",
    "steps_to_reproduce": [
      "Navigate to /overview",
      "Find 'Memory writes pending \u2014 vendor closed the job' section",
      "Click the 'Gegensprechanlage defekt \u2014 Einheit E1' link (ticket t-7832)",
      "Observe: ticket detail page loads with vendor job info but no 'Save to memory' or 'Skip' buttons"
    ],
    "expected": "Operator should see save/skip buttons to decide whether to write the vendor job outcome to context.md",
    "actual": "No memory action controls appear on the ticket detail page; memoryProposal is null in the API response",
    "file_hint": "frontend/apps/web/app/ticket/[id]/page.tsx"
  },
  {
    "id": "bug-002",
    "severity": "low",
    "category": "ux",
    "description": "Stale seeded error action cards visible before first re-run: On a fresh database (before reset), tickets t-7834 and t-7837 displayed an initial error action card 'get_ticket: error \u2014 Ticket GAR-121 not found' (using the ticket number instead of internal ID). These were seeded artifacts from a pre-fix run. After /api/dev/reset the issue disappears, but any deployment with old seeded data will confuse operators with visible error cards alongside correct ones.",
    "steps_to_reproduce": [
      "Deploy from a fresh seed without running /api/dev/reset",
      "Navigate to /ticket/t-7834",
      "Observe the first action card shows 'get_ticket: error \u2014 Ticket GAR-121 not found'"
    ],
    "expected": "No error action cards in freshly seeded database",
    "actual": "Error action card is visible (legacy from pre-Bug-003-fix data); correct card from second run appears alongside it",
    "file_hint": "backend/app/services/seed_service.py"
  },
  {
    "id": "bug-003",
    "severity": "low",
    "category": "ux",
    "description": "Chat at /chat does not use the selected property scope: When 'Gartenstra\u00dfe 42' is selected in the scope dropdown and user asks 'What is the phone number for this property?', Hermes responds with 'Please provide the property ID' instead of returning the phone number from context. The property scope selector appears non-functional for scoping the agent's context.",
    "steps_to_reproduce": [
      "Navigate to /chat",
      "Select 'Gartenstra\u00dfe 42' from the scope dropdown",
      "Ask: 'What is the phone number for this property?'",
      "Observe response: 'Please provide the property ID'"
    ],
    "expected": "Hermes uses the selected property scope and returns +49 30 5550 1042",
    "actual": "Hermes ignores the selected property and asks for a property ID",
    "file_hint": "backend/app/api/routes/chat.py or frontend/apps/web/app/chat/page.tsx"
  }
]

Evaluator notes: Sprint 2 is in strong shape. All four agent action types (vendor email, tenant response, owner financial report, human escalation) work end-to-end with streaming. The escalation flow is the most complex and works correctly: Confirm POSTs to /api/tickets/{id}/escalate with the real internal ID (Bug-003 fix confirmed), the ticket page shows the escalation notice after reload, and /escalations shows the 'Agent escalated' badge. Action cards render with proper type-specific styling (red border for escalations, amber for write proposals, muted for reads) and the 'Proposed (display only)' button correctly signals display-only actions. Code quality is clean with good separation between proposal factories and tool definitions. The three bugs found are all low severity — none block core sprint functionality. The memory workflow regression is the most notable gap: the 'memory pending' state surfaces in the overview but has no actionable UI on the ticket detail page.

## Instructions
1. Read CLAUDE.md and key source files to understand current patterns
2. Create the branch
3. Implement each feature in the order listed
4. Commit after each feature with format: [ai-builder/generator] description\nSprint: 2 | Feature: <id>
5. After ALL features are implemented, output the sprint contract as a ```json block

Set run_id to "20260426-031913" in the contract.
Set branch to "ai/sprint-2-agent-action-types-streaming-actions" in the contract.