## Self-Review Time

You just implemented Sprint 2: Agent Action Types & Streaming Actions.

Now switch to reviewer mode. Go through the self-critic checklist for every file you changed.

## Sprint Contract (what you implemented)
{
  "run_id": "20260426-031913",
  "sprint_number": 2,
  "branch": "ai/sprint-2-agent-action-types-streaming-actions",
  "features_implemented": [
    {
      "id": "s2-f1",
      "name": "Agent Email-to-Vendor Action",
      "files_changed": [
        "backend/app/services/agent_tools.py"
      ],
      "new_endpoints": [],
      "new_pages": [],
      "test_instructions": [
        "Open the Hermes chat (any property page with a vendor-coordination ticket)",
        "Ask: 'Process ticket t-7834 and draft a vendor email'",
        "Observe the SSE stream: the agent calls get_ticket, get_property_context, list_vendors, get_vendor, then calls send_vendor_email",
        "An action_proposal event fires with tool='send_vendor_email', email.to, email.subject, email.body filled in",
        "Alternatively: open ticket t-7834 detail page, click 'Run Hermes', watch action cards stream in \u2014 one should be a 'send_vendor_email' proposed card",
        "Expand the action card to see the full drafted email",
        "The card shows 'Proposed (display only)' since no actual send endpoint exists"
      ],
      "known_limitations": [
        "Email is displayed only \u2014 no actual SMTP send is performed",
        "The confirmEndpoint is null for send_vendor_email (display-only proposal)"
      ]
    },
    {
      "id": "s2-f2",
      "name": "Agent Tenant-Response Action",
      "files_changed": [
        "backend/app/services/agent_tools.py"
      ],
      "new_endpoints": [],
      "new_pages": [],
      "test_instructions": [
        "Open ticket t-7837 (GAR-123 \u2014 recycling inquiry from Herr Mayer, source=email)",
        "Click 'Run Hermes' on the ticket detail page",
        "Agent reads the ticket, loads property context (which contains recycling FAQ), then calls respond_to_tenant",
        "The proposed action card shows channel='email_reply' matching the ticket source=email",
        "Expand the card to see the drafted response in German referencing the property FAQ",
        "For ticket t-7838 (voice source), the agent should propose channel='sms'",
        "For ticket t-7839 (portal source), the agent should propose channel='portal_message'"
      ],
      "known_limitations": [
        "Channel selection is guided by agent prompt \u2014 LLM must correctly match source to channel",
        "Response is display-only, no actual SMS/portal integration"
      ]
    },
    {
      "id": "s2-f3",
      "name": "Agent Owner-Financial-Report Action",
      "files_changed": [
        "backend/app/services/agent_tools.py"
      ],
      "new_endpoints": [],
      "new_pages": [],
      "test_instructions": [
        "Open ticket t-7840 (GAR-125 \u2014 monthly cost summary request from WEG Beirat)",
        "Click 'Run Hermes' on the ticket detail page",
        "Agent reads ticket, loads property context (owner layer has financial reporting prefs), calls get_owner, list_units",
        "Agent then calls send_owner_report with ownerName='WEG Gartenstra\u00dfe 42', recipient='klein@weg-gartenstrasse.de', format='pdf_email'",
        "The proposed action card shows a compiled financial summary",
        "For ticket t-7841 (BRU-12 \u2014 invoice forwarding), agent should propose send_owner_report for Klaus Weber"
      ],
      "known_limitations": [
        "Financial data comes from seed data \u2014 real invoice amounts are from vendor_cases table",
        "Report is display-only, no actual email send"
      ]
    },
    {
      "id": "s2-f4",
      "name": "Agent Human-Escalation Action",
      "files_changed": [
        "backend/app/services/agent_tools.py",
        "backend/app/services/agent_runtime.py",
        "backend/app/models/tickets.py",
        "backend/app/schemas/tickets.py",
        "backend/app/schemas/agent_actions.py",
        "backend/app/api/routes/tickets.py",
        "backend/app/models/agent_actions.py",
        "backend/alembic/versions/0006_agent_actions_escalation.py",
        "frontend/apps/web/app/ticket/[id]/page.tsx",
        "frontend/apps/web/app/escalations/page.tsx",
        "frontend/apps/web/lib/types.ts"
      ],
      "new_endpoints": [
        {
          "method": "POST",
          "path": "/api/tickets/{ticket_id}/escalate",
          "body": {
            "reason": "string",
            "triggeringPolicy": "string (optional)",
            "recommendation": "string (optional)"
          }
        }
      ],
      "new_pages": [],
      "test_instructions": [
        "Open ticket t-7843 (GAR-126 \u2014 legal Abmahnung from lawyer Dr. M\u00fcller)",
        "Click 'Run Hermes' on the ticket detail page",
        "Agent reads ticket body (contains 'Abmahnung'), loads property context (escalation rules say legal matters \u2192 always escalate), then calls escalate_to_human",
        "An 'escalate_to_human' proposed action card appears in red with reason, triggering policy, and recommendation",
        "Click 'Confirm' on the escalation card \u2014 this POSTs to /api/tickets/{id}/escalate",
        "The ticket risk is set to 'high' and escalatedAt/escalationReason are stored",
        "Verify ticket now appears on the /escalations page with 'Agent escalated' badge",
        "Ticket t-7842 (LIN-7 \u2014 rent arrears 2 months) should also trigger escalation"
      ],
      "known_limitations": [
        "Escalation confirm sets risk=high and stores reason \u2014 ticket status unchanged",
        "The escalation action card has a working confirm button (unlike display-only email/response actions)"
      ]
    },
    {
      "id": "s2-f5",
      "name": "Streamable Action Cards in Ticket Detail",
      "files_changed": [
        "backend/app/api/routes/agent_run.py",
        "backend/app/models/agent_actions.py",
        "backend/app/schemas/agent_actions.py",
        "backend/app/models/__init__.py",
        "backend/app/api/routes/dev.py",
        "backend/app/main.py",
        "backend/alembic/versions/0006_agent_actions_escalation.py",
        "frontend/apps/web/app/ticket/[id]/page.tsx",
        "frontend/apps/web/components/action-card.tsx",
        "frontend/apps/web/lib/api.ts",
        "frontend/apps/web/lib/types.ts"
      ],
      "new_endpoints": [
        {
          "method": "POST",
          "path": "/api/tickets/{ticket_id}/agent-run",
          "body": {
            "promptHint": "string (optional)"
          }
        },
        {
          "method": "GET",
          "path": "/api/tickets/{ticket_id}/actions",
          "body": null
        }
      ],
      "new_pages": [],
      "test_instructions": [
        "Open any ticket detail page (e.g., /ticket/t-7843 or /ticket/t-7837)",
        "In the 'Hermes agent actions' card, click 'Run Hermes'",
        "Watch action cards stream in one-by-one as the agent runs",
        "Read-tool cards (context lookups, vendor reads) appear with a search icon and green 'done' badge",
        "Write-tool cards (email drafts, responses, escalations) appear with amber/red styling and 'proposed' badge",
        "Expand any proposed action card with the chevron to see full details",
        "For write actions with a confirmEndpoint (escalate_to_human), click 'Confirm' to execute",
        "After confirming escalation, the ticket's escalation badge appears in the header",
        "Reload the page \u2014 previous run's action cards are loaded from the database",
        "Click 'Re-run Hermes' to run the agent again (new run_id, cards animate in fresh)"
      ],
      "known_limitations": [
        "The streaming is SSE from POST \u2014 browser may require the page to remain open during run",
        "If OPENROUTER_API_KEY is not set, the agent falls back to stub mode (no tool calls, no action cards)",
        "Animation uses Tailwind animate-in which requires the tailwindcss-animate plugin"
      ]
    }
  ],
  "migrations_applied": [
    "backend/alembic/versions/0006_agent_actions_escalation.py"
  ],
  "how_to_verify": {
    "start_command": "cd backend && python run.py (port 8000) | cd frontend && pnpm dev (port 3000)",
    "base_url": "http://localhost:3000",
    "auth": {
      "description": "No authentication required. Single-presenter demo environment. Navigate directly to any ticket at /ticket/{id}."
    }
  },
  "regression_checks": [
    "POST /api/dev/reset still drops and re-seeds all tables including new agent_actions",
    "Existing ticket detail page still shows proposal card, vendor job, memory decision",
    "GET /api/tickets still returns all tickets with existing fields",
    "GET /api/escalations still returns high-risk tickets (now also with escalationReason/escalatedAt fields)",
    "Existing write tools (approve_ticket, reject_ticket, save_memory, skip_memory, propose_context_edit) still work in chat",
    "POST /api/chat still streams correctly with all 18 tools available",
    "Ticket approve/reject/memory flows still work from ticket detail page buttons"
  ]
}

Read each file you changed and review against the checklist in your system prompt.
Return ONLY the review JSON. No other text.

---

## Self-Review Time

You just implemented Sprint 2: Agent Action Types & Streaming Actions.

Now switch to reviewer mode. Go through the self-critic checklist for every file you changed.

## Sprint Contract (what you implemented)
{
  "run_id": "20260426-031913",
  "sprint_number": 2,
  "branch": "ai/sprint-2-agent-action-types-streaming-actions",
  "features_implemented": [
    {
      "id": "s2-f1",
      "name": "Agent Email-to-Vendor Action",
      "files_changed": [
        "backend/app/services/agent_tools.py"
      ],
      "new_endpoints": [],
      "new_pages": [],
      "test_instructions": [
        "Open the Hermes chat (any property page with a vendor-coordination ticket)",
        "Ask: 'Process ticket t-7834 and draft a vendor email'",
        "Observe the SSE stream: the agent calls get_ticket, get_property_context, list_vendors, get_vendor, then calls send_vendor_email",
        "An action_proposal event fires with tool='send_vendor_email', email.to, email.subject, email.body filled in",
        "Alternatively: open ticket t-7834 detail page, click 'Run Hermes', watch action cards stream in \u2014 one should be a 'send_vendor_email' proposed card",
        "Expand the action card to see the full drafted email",
        "The card shows 'Proposed (display only)' since no actual send endpoint exists"
      ],
      "known_limitations": [
        "Email is displayed only \u2014 no actual SMTP send is performed",
        "The confirmEndpoint is null for send_vendor_email (display-only proposal)"
      ]
    },
    {
      "id": "s2-f2",
      "name": "Agent Tenant-Response Action",
      "files_changed": [
        "backend/app/services/agent_tools.py"
      ],
      "new_endpoints": [],
      "new_pages": [],
      "test_instructions": [
        "Open ticket t-7837 (GAR-123 \u2014 recycling inquiry from Herr Mayer, source=email)",
        "Click 'Run Hermes' on the ticket detail page",
        "Agent reads the ticket, loads property context (which contains recycling FAQ), then calls respond_to_tenant",
        "The proposed action card shows channel='email_reply' matching the ticket source=email",
        "Expand the card to see the drafted response in German referencing the property FAQ",
        "For ticket t-7838 (voice source), the agent should propose channel='sms'",
        "For ticket t-7839 (portal source), the agent should propose channel='portal_message'"
      ],
      "known_limitations": [
        "Channel selection is guided by agent prompt \u2014 LLM must correctly match source to channel",
        "Response is display-only, no actual SMS/portal integration"
      ]
    },
    {
      "id": "s2-f3",
      "name": "Agent Owner-Financial-Report Action",
      "files_changed": [
        "backend/app/services/agent_tools.py"
      ],
      "new_endpoints": [],
      "new_pages": [],
      "test_instructions": [
        "Open ticket t-7840 (GAR-125 \u2014 monthly cost summary request from WEG Beirat)",
        "Click 'Run Hermes' on the ticket detail page",
        "Agent reads ticket, loads property context (owner layer has financial reporting prefs), calls get_owner, list_units",
        "Agent then calls send_owner_report with ownerName='WEG Gartenstra\u00dfe 42', recipient='klein@weg-gartenstrasse.de', format='pdf_email'",
        "The proposed action card shows a compiled financial summary",
        "For ticket t-7841 (BRU-12 \u2014 invoice forwarding), agent should propose send_owner_report for Klaus Weber"
      ],
      "known_limitations": [
        "Financial data comes from seed data \u2014 real invoice amounts are from vendor_cases table",
        "Report is display-only, no actual email send"
      ]
    },
    {
      "id": "s2-f4",
      "name": "Agent Human-Escalation Action",
      "files_changed": [
        "backend/app/services/agent_tools.py",
        "backend/app/services/agent_runtime.py",
        "backend/app/models/tickets.py",
        "backend/app/schemas/tickets.py",
        "backend/app/schemas/agent_actions.py",
        "backend/app/api/routes/tickets.py",
        "backend/app/models/agent_actions.py",
        "backend/alembic/versions/0006_agent_actions_escalation.py",
        "frontend/apps/web/app/ticket/[id]/page.tsx",
        "frontend/apps/web/app/escalations/page.tsx",
        "frontend/apps/web/lib/types.ts"
      ],
      "new_endpoints": [
        {
          "method": "POST",
          "path": "/api/tickets/{ticket_id}/escalate",
          "body": {
            "reason": "string",
            "triggeringPolicy": "string (optional)",
            "recommendation": "string (optional)"
          }
        }
      ],
      "new_pages": [],
      "test_instructions": [
        "Open ticket t-7843 (GAR-126 \u2014 legal Abmahnung from lawyer Dr. M\u00fcller)",
        "Click 'Run Hermes' on the ticket detail page",
        "Agent reads ticket body (contains 'Abmahnung'), loads property context (escalation rules say legal matters \u2192 always escalate), then calls escalate_to_human",
        "An 'escalate_to_human' proposed action card appears in red with reason, triggering policy, and recommendation",
        "Click 'Confirm' on the escalation card \u2014 this POSTs to /api/tickets/{id}/escalate",
        "The ticket risk is set to 'high' and escalatedAt/escalationReason are stored",
        "Verify ticket now appears on the /escalations page with 'Agent escalated' badge",
        "Ticket t-7842 (LIN-7 \u2014 rent arrears 2 months) should also trigger escalation"
      ],
      "known_limitations": [
        "Escalation confirm sets risk=high and stores reason \u2014 ticket status unchanged",
        "The escalation action card has a working confirm button (unlike display-only email/response actions)"
      ]
    },
    {
      "id": "s2-f5",
      "name": "Streamable Action Cards in Ticket Detail",
      "files_changed": [
        "backend/app/api/routes/agent_run.py",
        "backend/app/models/agent_actions.py",
        "backend/app/schemas/agent_actions.py",
        "backend/app/models/__init__.py",
        "backend/app/api/routes/dev.py",
        "backend/app/main.py",
        "backend/alembic/versions/0006_agent_actions_escalation.py",
        "frontend/apps/web/app/ticket/[id]/page.tsx",
        "frontend/apps/web/components/action-card.tsx",
        "frontend/apps/web/lib/api.ts",
        "frontend/apps/web/lib/types.ts"
      ],
      "new_endpoints": [
        {
          "method": "POST",
          "path": "/api/tickets/{ticket_id}/agent-run",
          "body": {
            "promptHint": "string (optional)"
          }
        },
        {
          "method": "GET",
          "path": "/api/tickets/{ticket_id}/actions",
          "body": null
        }
      ],
      "new_pages": [],
      "test_instructions": [
        "Open any ticket detail page (e.g., /ticket/t-7843 or /ticket/t-7837)",
        "In the 'Hermes agent actions' card, click 'Run Hermes'",
        "Watch action cards stream in one-by-one as the agent runs",
        "Read-tool cards (context lookups, vendor reads) appear with a search icon and green 'done' badge",
        "Write-tool cards (email drafts, responses, escalations) appear with amber/red styling and 'proposed' badge",
        "Expand any proposed action card with the chevron to see full details",
        "For write actions with a confirmEndpoint (escalate_to_human), click 'Confirm' to execute",
        "After confirming escalation, the ticket's escalation badge appears in the header",
        "Reload the page \u2014 previous run's action cards are loaded from the database",
        "Click 'Re-run Hermes' to run the agent again (new run_id, cards animate in fresh)"
      ],
      "known_limitations": [
        "The streaming is SSE from POST \u2014 browser may require the page to remain open during run",
        "If OPENROUTER_API_KEY is not set, the agent falls back to stub mode (no tool calls, no action cards)",
        "Animation uses Tailwind animate-in which requires the tailwindcss-animate plugin"
      ]
    }
  ],
  "migrations_applied": [
    "backend/alembic/versions/0006_agent_actions_escalation.py"
  ],
  "how_to_verify": {
    "start_command": "cd backend && python run.py (port 8000) | cd frontend && pnpm dev (port 3000)",
    "base_url": "http://localhost:3000",
    "auth": {
      "description": "No authentication required. Single-presenter demo environment. Navigate directly to any ticket at /ticket/{id}."
    }
  },
  "regression_checks": [
    "POST /api/dev/reset still drops and re-seeds all tables including new agent_actions",
    "Existing ticket detail page still shows proposal card, vendor job, memory decision",
    "GET /api/tickets still returns all tickets with existing fields",
    "GET /api/escalations still returns high-risk tickets (now also with escalationReason/escalatedAt fields)",
    "Existing write tools (approve_ticket, reject_ticket, save_memory, skip_memory, propose_context_edit) still work in chat",
    "POST /api/chat still streams correctly with all 18 tools available",
    "Ticket approve/reject/memory flows still work from ticket detail page buttons"
  ]
}

Read each file you changed and review against the checklist in your system prompt.
Return ONLY the review JSON. No other text.

---

## Fix Self-Critic Issues

Fix these issues identified in the self-review:

[
  {
    "severity": "high",
    "category": "error_handling",
    "file": "frontend/apps/web/app/ticket/[id]/page.tsx",
    "line_range": "89-109",
    "description": "startAgentRun() has no `finally` block. If the SSE stream closes without emitting a `done` event (e.g., server error before the final yield, or network drop), agentRunning stays true forever \u2014 the 'running\u2026' badge never clears and the Run button remains disabled. The AbortError path is fine because stopAgentRun() sets agentRunning=false before the abort fires, but normal stream-close-without-done is unhandled.",
    "suggested_fix": "Wrap the await in a try/finally and move setAgentRunning(false) into the finally block. Remove the duplicate setAgentRunning(false) calls from onDone, onError, and the catch block.\n\nasync function startAgentRun() {\n  if (agentRunning) return;\n  setAgentRunning(true);\n  setAgentDone(false);\n  setAgentText(\"\");\n  const ctrl = new AbortController();\n  abortRef.current = ctrl;\n  try {\n    await agentRun.stream(id, {\n      onActionCard: upsertAction,\n      onDelta: (text) => setAgentText((s) => s + text),\n      onDone: (payload) => { setAgentDone(true); refetchTicket(); },\n      onError: (err) => { pushToast({ title: \"Agent run failed\", description: err instanceof Error ? err.message : String(err), variant: \"destructive\" }); },\n      signal: ctrl.signal,\n    });\n  } catch (err) {\n    if (!(err instanceof Error && err.name === \"AbortError\")) {\n      pushToast({ title: \"Agent run failed\", description: String(err), variant: \"destructive\" });\n    }\n  } finally {\n    setAgentRunning(false);\n  }\n}",
    "auto_fixable": true
  },
  {
    "severity": "medium",
    "category": "pattern",
    "file": "frontend/apps/web/components/action-card.tsx",
    "line_range": "95-115",
    "description": "handleConfirm() duplicates the BASE URL logic from api.ts (hardcodes the `typeof process !== 'undefined' && process.env?.NEXT_PUBLIC_BACKEND_URL || 'http://localhost:8000'` pattern) and uses raw fetch() instead of the project's request<T>() utility. This means (1) the backend URL is defined in two places \u2014 a change to NEXT_PUBLIC_BACKEND_URL in api.ts won't automatically apply here, and (2) error responses are not parsed into the project's standard ApiError envelope, so the friendly error message system is bypassed.",
    "suggested_fix": "Export `buildUrl` from api.ts, or better: add a generic `confirmAction(endpoint: string, method: string, body: unknown)` helper in api.ts that wraps request<T>(), and import it in action-card.tsx. Shortest fix: import the BASE constant by extracting it to a shared lib/config.ts file and importing it in both api.ts and action-card.tsx.",
    "auto_fixable": true
  },
  {
    "severity": "medium",
    "category": "error_handling",
    "file": "frontend/apps/web/components/action-card.tsx",
    "line_range": "95-120",
    "description": "handleConfirm() uses ad-hoc error parsing: `res.json().catch(() => ({ error: res.statusText }))`. This does not use the project's standardized ApiErrorEnvelope `{error, code, status}` shape from api.ts, so the friendly error messages from friendlyApiError() are never surfaced. A 409 CONFLICT response from the escalate endpoint (e.g., ticket already resolved) will show a raw error string instead of the user-friendly message.",
    "suggested_fix": "Replace the raw fetch with a call to the typed API method when possible. For the generic case, at minimum parse the response using the same envelope shape as api.ts: try { const parsed = await res.json(); if ('error' in parsed) throw new ApiError(parsed, endpoint); } catch { ... }. Import ApiError from @/lib/api.",
    "auto_fixable": true
  },
  {
    "severity": "low",
    "category": "project_specific",
    "file": "backend/app/api/routes/agent_run.py",
    "line_range": "158-167",
    "description": "When an AgentAction is first created with db.add(action); db.flush(), the created_at field is None in Python because it is populated by a server_default=sa.func.now() which only resolves on the database side. The first SSE action_card event emitted (status='thinking') will have createdAt: null. The UI handles this gracefully (no crash), but the timestamp is missing on the live 'thinking' card. It only populates after agent_runtime's db.commit() triggers SQLAlchemy expiry and a lazy reload.",
    "suggested_fix": "After db.flush(), explicitly set a Python-side fallback: `from datetime import datetime, timezone; action.created_at = action.created_at or datetime.now(timezone.utc)`. This ensures the first SSE emit has a real timestamp without requiring an extra DB roundtrip.",
    "auto_fixable": true
  },
  {
    "severity": "low",
    "category": "error_handling",
    "file": "backend/app/api/routes/agent_run.py",
    "line_range": "119-133",
    "description": "POST /api/tickets/{id}/agent-run always returns 200 OK even if the ticket does not exist. The not-found check happens inside the generator and emits an SSE `error` event. All other ticket endpoints in tickets.py return a proper 404 JSON response via _get_ticket_or_404(). While this is consistent with chat.py behavior, it breaks the contract a client would expect for an unknown ticket ID.",
    "suggested_fix": "Add a pre-flight ticket existence check before starting the StreamingResponse: query the ticket outside the generator using a short-lived DB session, raise HTTPException(404) if not found. This matches the pattern of every other ticket route in the codebase.",
    "auto_fixable": true
  }
]

For each fix:
1. Make the code change
2. Commit with format: [ai-builder/self-fix] <description>\nSprint: N | Self-critic fix

Fix ALL issues listed above.

## Final Step (CRITICAL)
After all fixes, run `git status`. If there are ANY uncommitted changes:
```
git add -A
git commit -m "[ai-builder/self-fix] finalize fixes
Sprint: N | Self-critic final"
```
This commit triggers session tracking hooks — do not skip it.

---

## Fix Self-Critic Issues

Fix these issues identified in the self-review:

[
  {
    "severity": "high",
    "category": "error_handling",
    "file": "frontend/apps/web/app/ticket/[id]/page.tsx",
    "line_range": "89-109",
    "description": "startAgentRun() has no `finally` block. If the SSE stream closes without emitting a `done` event (e.g., server error before the final yield, or network drop), agentRunning stays true forever \u2014 the 'running\u2026' badge never clears and the Run button remains disabled. The AbortError path is fine because stopAgentRun() sets agentRunning=false before the abort fires, but normal stream-close-without-done is unhandled.",
    "suggested_fix": "Wrap the await in a try/finally and move setAgentRunning(false) into the finally block. Remove the duplicate setAgentRunning(false) calls from onDone, onError, and the catch block.\n\nasync function startAgentRun() {\n  if (agentRunning) return;\n  setAgentRunning(true);\n  setAgentDone(false);\n  setAgentText(\"\");\n  const ctrl = new AbortController();\n  abortRef.current = ctrl;\n  try {\n    await agentRun.stream(id, {\n      onActionCard: upsertAction,\n      onDelta: (text) => setAgentText((s) => s + text),\n      onDone: (payload) => { setAgentDone(true); refetchTicket(); },\n      onError: (err) => { pushToast({ title: \"Agent run failed\", description: err instanceof Error ? err.message : String(err), variant: \"destructive\" }); },\n      signal: ctrl.signal,\n    });\n  } catch (err) {\n    if (!(err instanceof Error && err.name === \"AbortError\")) {\n      pushToast({ title: \"Agent run failed\", description: String(err), variant: \"destructive\" });\n    }\n  } finally {\n    setAgentRunning(false);\n  }\n}",
    "auto_fixable": true
  },
  {
    "severity": "medium",
    "category": "pattern",
    "file": "frontend/apps/web/components/action-card.tsx",
    "line_range": "95-115",
    "description": "handleConfirm() duplicates the BASE URL logic from api.ts (hardcodes the `typeof process !== 'undefined' && process.env?.NEXT_PUBLIC_BACKEND_URL || 'http://localhost:8000'` pattern) and uses raw fetch() instead of the project's request<T>() utility. This means (1) the backend URL is defined in two places \u2014 a change to NEXT_PUBLIC_BACKEND_URL in api.ts won't automatically apply here, and (2) error responses are not parsed into the project's standard ApiError envelope, so the friendly error message system is bypassed.",
    "suggested_fix": "Export `buildUrl` from api.ts, or better: add a generic `confirmAction(endpoint: string, method: string, body: unknown)` helper in api.ts that wraps request<T>(), and import it in action-card.tsx. Shortest fix: import the BASE constant by extracting it to a shared lib/config.ts file and importing it in both api.ts and action-card.tsx.",
    "auto_fixable": true
  },
  {
    "severity": "medium",
    "category": "error_handling",
    "file": "frontend/apps/web/components/action-card.tsx",
    "line_range": "95-120",
    "description": "handleConfirm() uses ad-hoc error parsing: `res.json().catch(() => ({ error: res.statusText }))`. This does not use the project's standardized ApiErrorEnvelope `{error, code, status}` shape from api.ts, so the friendly error messages from friendlyApiError() are never surfaced. A 409 CONFLICT response from the escalate endpoint (e.g., ticket already resolved) will show a raw error string instead of the user-friendly message.",
    "suggested_fix": "Replace the raw fetch with a call to the typed API method when possible. For the generic case, at minimum parse the response using the same envelope shape as api.ts: try { const parsed = await res.json(); if ('error' in parsed) throw new ApiError(parsed, endpoint); } catch { ... }. Import ApiError from @/lib/api.",
    "auto_fixable": true
  },
  {
    "severity": "low",
    "category": "project_specific",
    "file": "backend/app/api/routes/agent_run.py",
    "line_range": "158-167",
    "description": "When an AgentAction is first created with db.add(action); db.flush(), the created_at field is None in Python because it is populated by a server_default=sa.func.now() which only resolves on the database side. The first SSE action_card event emitted (status='thinking') will have createdAt: null. The UI handles this gracefully (no crash), but the timestamp is missing on the live 'thinking' card. It only populates after agent_runtime's db.commit() triggers SQLAlchemy expiry and a lazy reload.",
    "suggested_fix": "After db.flush(), explicitly set a Python-side fallback: `from datetime import datetime, timezone; action.created_at = action.created_at or datetime.now(timezone.utc)`. This ensures the first SSE emit has a real timestamp without requiring an extra DB roundtrip.",
    "auto_fixable": true
  },
  {
    "severity": "low",
    "category": "error_handling",
    "file": "backend/app/api/routes/agent_run.py",
    "line_range": "119-133",
    "description": "POST /api/tickets/{id}/agent-run always returns 200 OK even if the ticket does not exist. The not-found check happens inside the generator and emits an SSE `error` event. All other ticket endpoints in tickets.py return a proper 404 JSON response via _get_ticket_or_404(). While this is consistent with chat.py behavior, it breaks the contract a client would expect for an unknown ticket ID.",
    "suggested_fix": "Add a pre-flight ticket existence check before starting the StreamingResponse: query the ticket outside the generator using a short-lived DB session, raise HTTPException(404) if not found. This matches the pattern of every other ticket route in the codebase.",
    "auto_fixable": true
  }
]

For each fix:
1. Make the code change
2. Commit with format: [ai-builder/self-fix] <description>\nSprint: N | Self-critic fix

Fix ALL issues listed above.

## Final Step (CRITICAL)
After all fixes, run `git status`. If there are ANY uncommitted changes:
```
git add -A
git commit -m "[ai-builder/self-fix] finalize fixes
Sprint: N | Self-critic final"
```
This commit triggers session tracking hooks — do not skip it.